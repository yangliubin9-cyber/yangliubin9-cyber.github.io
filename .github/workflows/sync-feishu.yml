name: Sync Feishu Posts

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      FEISHU_OPEN_BASE_URL: ${{ secrets.FEISHU_OPEN_BASE_URL }}
      FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
      FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
      FEISHU_USER_REFRESH_TOKEN: ${{ secrets.FEISHU_USER_REFRESH_TOKEN }}
      GH_ACTIONS_SECRET_WRITER_TOKEN: ${{ secrets.GH_ACTIONS_SECRET_WRITER_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate Feishu secrets
        shell: bash
        run: |
          if [ -z "${FEISHU_APP_ID}" ] || [ -z "${FEISHU_APP_SECRET}" ]; then
            echo "::error::Missing FEISHU_APP_ID or FEISHU_APP_SECRET in GitHub Actions secrets."
            exit 1
          fi
          if [ -z "${FEISHU_USER_REFRESH_TOKEN}" ]; then
            echo "::error::Missing FEISHU_USER_REFRESH_TOKEN in GitHub Actions secrets. Personal Feishu folders require a user refresh token."
            exit 1
          fi
          if [ -z "${GH_ACTIONS_SECRET_WRITER_TOKEN}" ]; then
            echo "::error::Missing GH_ACTIONS_SECRET_WRITER_TOKEN in GitHub Actions secrets. This workflow auto-rotates FEISHU_USER_REFRESH_TOKEN after each successful refresh."
            exit 1
          fi

      - name: Sync Feishu posts
        id: sync_feishu
        shell: bash
        run: |
          set -o pipefail
          log_file="$(mktemp)"
          if npm run sync:feishu 2>&1 | tee "$log_file"; then
            exit 0
          fi
          if grep -q 'FEISHU_USER_REFRESH_TOKEN is invalid or expired' "$log_file"; then
            echo "::error::FEISHU_USER_REFRESH_TOKEN is invalid or expired. Run npm run auth:feishu locally, update GitHub Actions secret FEISHU_USER_REFRESH_TOKEN, then rerun Sync Feishu Posts."
          fi
          exit 1

      - name: Persist rotated Feishu refresh token
        if: ${{ steps.sync_feishu.outputs.feishu_refresh_token_rotated == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GH_ACTIONS_SECRET_WRITER_TOKEN }}
          NEXT_FEISHU_REFRESH_TOKEN: ${{ steps.sync_feishu.outputs.next_feishu_refresh_token }}
        shell: bash
        run: |
          if [ -z "${NEXT_FEISHU_REFRESH_TOKEN}" ]; then
            echo "::error::Feishu sync reported refresh token rotation, but no replacement token was captured."
            exit 1
          fi
          if ! command -v gh >/dev/null 2>&1; then
            echo "::error::GitHub CLI is not available on the runner, so FEISHU_USER_REFRESH_TOKEN cannot be updated."
            exit 1
          fi
          gh secret set FEISHU_USER_REFRESH_TOKEN --repo "${GITHUB_REPOSITORY}" --body "${NEXT_FEISHU_REFRESH_TOKEN}"
          echo "Updated FEISHU_USER_REFRESH_TOKEN in GitHub Actions secrets."

      - name: Verify build
        env:
          ASTRO_TELEMETRY_DISABLED: '1'
        run: npm run build

      - name: Commit synced content
        run: |
          if git diff --quiet; then
            echo "No synced content changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/content/posts
          git commit -m "Sync Feishu posts"
          git push
