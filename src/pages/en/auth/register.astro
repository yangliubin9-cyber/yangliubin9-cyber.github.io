---
import RouteRedirectPage from '../../../components/RouteRedirectPage.astro';
import { getUiText } from '../../../data/ui';
import { localePath } from '../../../lib/routing';

const redirectUi = getUiText('en').redirect;
const redirectTarget = localePath('en');
---

<RouteRedirectPage
  lang="en"
  title={redirectUi.title}
  redirectTarget={redirectTarget}
  message={redirectUi.message}
/>
